import logoURL from './logo.png'
import logoLockupURL from './logo-lockup.png'

/** Ring brand mark (transparent background) — sidebar, welcome screen. */
export const appLogoURL = logoURL
/** Horizontal lockup (mark + 石间 + SHEJANE) — auth/login page. */
export const appLogoLockupURL = logoLockupURL
